# RSS Intelligence SSL Configuration

# Your domain name (required for production SSL)
SSL_DOMAIN=your-domain.com

# Your email for Let's Encrypt notifications
SSL_EMAIL=your-email@example.com

# Use Let's Encrypt staging server for testing (set to 0 for production)
SSL_STAGING=1

# Database configuration
POSTGRES_USER=rss
POSTGRES_PASSWORD=changeme
POSTGRES_DB=rssintel

# FreshRSS configuration
FRESHRSS_ADMIN_USER=admin
FRESHRSS_API_USER=ai
FRESHRSS_API_PASS=strongpassword

# Other configuration...
SCORING_HALF_LIFE_HOURS=36
SCORING_STAR_THRESHOLD=80
SCORING_INTEREST_THRESHOLD=60
SCHEDULER_ENABLED=true